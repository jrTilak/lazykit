---
title: countBy
description: Counts array values by a derived property key.
category: Arrays
kind: function
source: registry/functions/countBy.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="countBy" />

## Download

<DownloadCommands name="countBy" />

## Examples

<UtilityExamples name="countBy" />

## Props

- `array` (`readonly T[]`) — Values to count.
- `getKey` (`(value, index) => PropertyKey`) — Selects a bucket.
- Returns a null-prototype count record.
