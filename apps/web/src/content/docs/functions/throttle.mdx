---
title: throttle
description: Runs immediately, then drops calls until a specified interval has elapsed.
category: Functional
kind: function
source: registry/functions/throttle.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="throttle" />

## Download

<DownloadCommands name="throttle" />

## Examples

<UtilityExamples name="throttle" />

## Props

- `fn` (`(...args: A) => R`) — Function invoked on the leading call of each interval.
- `intervalMs` (`number`) — Finite, non-negative interval in milliseconds.

### Returns

A function returning `R` when invoked or `undefined` when dropped, plus `reset()` to reopen the interval immediately.
