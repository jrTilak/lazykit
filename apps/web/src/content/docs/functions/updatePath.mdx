---
title: updatePath
description: Immutably updates a value at an object path.
category: Objects
kind: function
source: registry/functions/updatePath.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="updatePath" />

## Download

<DownloadCommands name="updatePath" />

## Examples

<UtilityExamples name="updatePath" />

## Props

- `object` (`T`) — Root object or array.
- `path` — Non-empty dot path or segment array.
- `update` (`(current: unknown) => unknown`) — Receives the current value or `undefined`.
