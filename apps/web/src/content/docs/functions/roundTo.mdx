---
title: roundTo
description: Rounds finite numbers safely across decimal positions and extreme magnitudes.
category: Numbers
kind: function
source: registry/functions/roundTo.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="roundTo" />

## Download

<DownloadCommands name="roundTo" />

## Examples

<UtilityExamples name="roundTo" />

## Props

- `value` (`number`) — Finite number to round.
- `precision` (`number`) — Integer from `-100` to `100`; defaults to `0`.
- Negative precision rounds positions left of the decimal point.
- Halfway values follow `Math.round`, toward positive infinity, including negative ties.
- Negative results rounded to zero preserve `-0`.
- Extreme positive precision returns the original finite value when the requested
  decimal place is finer than the number can represent; no finite input silently
  produces `NaN` or infinity.
