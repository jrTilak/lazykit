---
title: mapObject
description: Transforms each own enumerable value while preserving its property key.
category: Objects
kind: function
source: registry/functions/mapObject.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="mapObject" />

## Download

<DownloadCommands name="mapObject" />

## Examples

<UtilityExamples name="mapObject" />

## Props

- `object` (`T`) — Object whose own enumerable values should be transformed.
- `transform` (`(value, key, object) => U`) — Produces the replacement value for each property.

### Returns

`{ [K in keyof T]: U }` — A new object with the same enumerable keys and transformed values.
