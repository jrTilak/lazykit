---
title: intersectionBy
description: Returns the first value for each derived key shared by all arrays.
category: Arrays
kind: function
source: registry/functions/intersectionBy.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="intersectionBy" />

## Download

<DownloadCommands name="intersectionBy" />

## Examples

<UtilityExamples name="intersectionBy" />

## Props

- `array` (`readonly T[]`) — The ordered source array.
- `others` (`readonly (readonly T[])[]`) — Arrays that must contain the key.
- `getKey` (`(value: T) => Key`) — Produces comparison keys.
