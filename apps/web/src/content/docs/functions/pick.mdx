---
title: pick
description: Copies a selected set of properties from an object.
category: Objects
kind: function
source: registry/functions/pick.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="pick" />

## Download

<DownloadCommands name="pick" />

## Examples

<UtilityExamples name="pick" />

## Props

- `object` (`T`) — Object to read from.
- `keys` (`readonly K[]`) — String, number, or symbol keys to copy.

### Returns

`Pick<T, K>` — A new object containing the selected properties.
