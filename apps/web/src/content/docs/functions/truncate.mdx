---
title: truncate
description: Truncates text to a maximum grapheme length including its marker.
category: Strings
kind: function
source: registry/functions/truncate.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="truncate" />

## Download

<DownloadCommands name="truncate" />

## Examples

<UtilityExamples name="truncate" />

## Props

- `value` (`string`) — Text to shorten.
- `maxLength` (`number`) — Non-negative safe-integer maximum number of visible characters.
- `omission` (`string`) — Marker included within the maximum, defaulting to `…`.
- Uses `Intl.Segmenter` to preserve emoji sequences, flags, and combining marks. Environments without it fall back to Unicode code points.
