---
title: filterObject
description: Keeps the own enumerable properties that satisfy a predicate.
category: Objects
kind: function
source: registry/functions/filterObject.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="filterObject" />

## Download

<DownloadCommands name="filterObject" />

## Examples

<UtilityExamples name="filterObject" />

## Props

- `object` (`T`) — Object whose own enumerable properties should be tested.
- `predicate` (`(value, key, object) => boolean`) — Returns `true` for properties to retain.

### Returns

`Partial<T>` — A new object containing the matching string and symbol properties.
