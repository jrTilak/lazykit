---
title: useMediaQuery
description: Subscribes to a CSS media query with a hydration-safe fallback.
category: Web APIs
kind: hook
source: registry/react-hooks/useMediaQuery.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="useMediaQuery" kind="hook" />

## Download

<DownloadCommands name="useMediaQuery" kind="hook" />

## Examples

<UtilityExamples name="useMediaQuery" kind="hook" />

## Props

- `query` (`string`) — CSS media query passed to `matchMedia`.
- `options.defaultValue` (`boolean`) — Used during SSR or without matchMedia.

### Returns

Whether the media query currently matches.
