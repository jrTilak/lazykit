---
title: useFocusWithin
description: Tracks whether focus is on an element or one of its descendants.
category: DOM
kind: hook
source: registry/react-hooks/useFocusWithin.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="useFocusWithin" kind="hook" />

## Download

<DownloadCommands name="useFocusWithin" kind="hook" />

## Examples

<UtilityExamples name="useFocusWithin" kind="hook" />

## Props

- `enabled` (`boolean`) — Enables focus listeners. Defaults to `true`.
- `ElementType` — Generic HTML element type attached to the callback ref.

### Returns

`UseFocusWithinReturn<ElementType>` with `ref` and `isFocusWithin`.
