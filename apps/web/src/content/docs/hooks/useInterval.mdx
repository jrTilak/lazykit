---
title: useInterval
description: Repeatedly invokes the latest callback and cleans up whenever its delay changes.
category: Timing
kind: hook
source: registry/react-hooks/useInterval.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="useInterval" kind="hook" />

## Download

<DownloadCommands name="useInterval" kind="hook" />

## Examples

<UtilityExamples name="useInterval" kind="hook" />

## Props

- `callback` (`() => void`) — Latest function to invoke on every tick.
- `intervalMs` (`number | null`) — Finite, non-negative interval. `null` pauses the interval; `0` is valid.

### Returns

`void`. Callback changes do not recreate the interval. Delay changes and unmounting clean up the previous timer.
