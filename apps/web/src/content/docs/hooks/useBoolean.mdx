---
title: useBoolean
description: Manages a boolean with explicit true, false, and toggle controls.
category: State
kind: hook
source: registry/react-hooks/useBoolean.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="useBoolean" kind="hook" />

## Download

<DownloadCommands name="useBoolean" kind="hook" />

## Examples

<UtilityExamples name="useBoolean" kind="hook" />

## Props

- `initialValue` (`boolean`, default: `false`) — Initial state used on mount.

### Returns

`UseBooleanResult` with `value`, React-compatible `setValue`, `setTrue`,
`setFalse`, and `toggle`. Every control keeps a stable identity.
