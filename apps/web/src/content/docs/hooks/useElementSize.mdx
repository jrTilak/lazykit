---
title: useElementSize
description: Measures an element with ResizeObserver and follows node swaps.
category: DOM
kind: hook
source: registry/react-hooks/useElementSize.ts
---

import DownloadCommands from "../../../components/DownloadCommands.astro";
import UtilityExamples from "../../../components/UtilityExamples.astro";
import UtilitySource from "../../../components/UtilitySource.astro";

<p class="utility-description">{frontmatter.description}</p>

## Code

<UtilitySource name="useElementSize" kind="hook" />

## Download

<DownloadCommands name="useElementSize" kind="hook" />

## Examples

<UtilityExamples name="useElementSize" kind="hook" />

## Props

- `options.box` (`ResizeObserverBoxOptions`) — Requested observation box.
- `ElementType` — Generic element type attached to the callback ref.

### Returns

`UseElementSizeReturn<ElementType>` with `ref`, dimensions, and `isSupported`.
